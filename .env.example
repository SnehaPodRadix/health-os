# Google Gemini API key — used server-side to extract text from uploaded records.
# Required. Get one from Google AI Studio.
GEMINI_API_KEY=

# Optional: override the Gemini model (default: gemini-3.1-flash-lite).
GEMINI_MODEL=
